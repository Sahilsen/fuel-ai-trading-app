import { Provider, WalletUnlocked, TransactionRequest } from 'fuels';
import { FuelWalletClient } from './FuelWalletClient.mjs';
import '@goat-sdk/core';

type FuelKeypairWalletClientCtorParams = {
    privateKey: string;
    provider: Provider;
};
declare class FuelKeypairWalletClient extends FuelWalletClient {
    readonly params: FuelKeypairWalletClientCtorParams;
    wallet: WalletUnlocked;
    constructor(params: FuelKeypairWalletClientCtorParams);
    getAddress(): string;
    signMessage(message: string): Promise<{
        signature: string;
    }>;
    transfer(to: string, amount: string): Promise<{
        hash: string;
    }>;
    sendTransaction(transaction: TransactionRequest): Promise<{
        hash: string;
    }>;
}
declare function fuel({ privateKey, provider }: FuelKeypairWalletClientCtorParams): FuelKeypairWalletClient;

export { FuelKeypairWalletClient, type FuelKeypairWalletClientCtorParams, fuel };
