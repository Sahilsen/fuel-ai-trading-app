import { WalletClientBase } from '@goat-sdk/core';
import { Provider, TransactionRequest } from 'fuels';

type FuelWalletCtorParams = {
    provider: Provider;
};
declare abstract class FuelWalletClient extends WalletClientBase {
    readonly params: FuelWalletCtorParams;
    private provider;
    constructor(params: FuelWalletCtorParams);
    getChain(): {
        readonly type: "fuel";
    };
    getProvider(): Provider;
    abstract sendTransaction(transaction: TransactionRequest): Promise<{
        hash: string;
    }>;
    abstract transfer(to: string, amount: string): Promise<{
        hash: string;
    }>;
    balanceOf(address: string): Promise<{
        decimals: number;
        symbol: string;
        name: string;
        value: string;
        inBaseUnits: string;
    }>;
}

export { FuelWalletClient, type FuelWalletCtorParams };
