import * as _goat_sdk_core from '@goat-sdk/core';
import { PluginBase, Chain } from '@goat-sdk/core';
import { z } from 'zod';
import { FuelWalletClient } from './FuelWalletClient.js';
import 'fuels';

declare class SendFuelETHPlugin extends PluginBase<FuelWalletClient> {
    constructor();
    supportsChain(chain: Chain): chain is _goat_sdk_core.FuelChain;
    getTools(walletClient: FuelWalletClient): {
        execute(parameters: {
            to: string;
            amount: string;
        }): string | Promise<string>;
        readonly name: string;
        readonly description: string;
        readonly parameters: z.ZodObject<{
            to: z.ZodString;
            amount: z.ZodString;
        }, "strip", z.ZodTypeAny, {
            to: string;
            amount: string;
        }, {
            to: string;
            amount: string;
        }>;
    }[];
}
declare const sendFuelETH: () => SendFuelETHPlugin;

export { SendFuelETHPlugin, sendFuelETH };
